# references/ — index

Supporting material for the graph-context-sync protocol. `SKILL.md` is the executable protocol; these files hold
what a maintainer needs but a session does not need on every invocation.

| File | Contents |
|------|----------|
| `10-data-model.md` | Root, `config`, node fields, edge direction conventions, hierarchy, invariants enforced by R1 |
| `40-decision-register.md` | Public decision register D1–D25: what was decided, what it superseded, why |
| `sync-source.md` | Where these files are maintained from and how to keep them in sync |

Authoritative shapes: `../schema/graph_schema.json` (JSON Schema draft 2020-12).
